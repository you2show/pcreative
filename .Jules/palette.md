## 2025-01-24 - Animated Language Toggle & Header A11y
**Learning:** Adding a language toggle using Framer Motion's `AnimatePresence` and `layout` props provides a high-delight micro-interaction that makes the transition feel physical rather than just a text swap. Haptic feedback on language change (distinct pattern) reinforces the significance of the action.
**Action:** Use `AnimatePresence` for text-swapping UI elements to avoid abrupt content jumps. Always integrate language toggles into both desktop and mobile navigation paths.
