// Supabase has been replaced by GitHub JSON + imgbb.
// These exports are kept as no-ops for any remaining references.
export const DEFAULT_SUPABASE_URL = '';
export const DEFAULT_SUPABASE_KEY = '';

export const supabase = null;

export const getSupabaseClient = (): null => null;
